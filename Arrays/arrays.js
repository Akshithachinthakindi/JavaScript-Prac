// let marks = {
//     stud: 12,
//     std2: 133,
//     std3: 1334
// };

//  the above is the syntax for object
// and in objects we have key-->value pair type of syntax

// let markss = [22,23,34,45,56,67];
// console.log(markss);
// console.log(markss.length);

//  in arrays we have index type of syntax
// arrays are mutable in javascript


// iterating through the arrays

let flowers = ["sunflowers", "daisies", "roses", "lilies", "tulips"];

for(let i of flowers) {
    console.log(`${i} -> ${flowers[i]}`);
}
